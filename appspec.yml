version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/Portfolio-backend
    overwrite: yes
hooks:
  ApplicationStop:
    - location: scripts/stop.sh
      timeout: 60
      runas: ubuntu
  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: ubuntu